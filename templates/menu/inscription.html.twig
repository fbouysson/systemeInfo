<!DOCTYPE html>
<html lang="fr">
<link rel="stylesheet" href="{{asset('css/bootstrap.css')}}">
<link rel="stylesheet" href="{{asset('css/bootstrap-grid.css')}}">
<link rel="stylesheet" href="{{asset('css/bootstrap-reboot.css')}}">
<link rel="stylesheet" href="{{asset('css/inscription.css')}}">
<link rel="shortcut icon" type="image/x-icon" href="{{ asset('img/onglet.png') }}" />
<head>
    <meta charset="UTF-8"/>
    <title>UCO : inscription</title>
</head>
<body>
<div>
    <h1>Ultimate Chat Online</h1>
    <br>
    <div class="row" style="text-align: center">
        <div class="col-12" style="display: block;">
            {{ form_start(form, {'attr': {'id': 'formInscription'}}) }}
            <div class="left">
                <div class="col-12 inputInsc">
                    {{ form_row(form.user_nom) }}
                </div>

                <div class="col-12 inputInsc">
                    {{ form_row(form.user_prenom) }}
                </div>

                <div class="col-12 inputInsc">
                    {{ form_row(form.user_email) }}
                </div>
            </div>

            <div class="right">
                <div class="col-12 inputInsc">
                    {{ form_row(form.user_username) }}
                </div>

                <div class="col-12 inputInsc">
                    {{ form_row(form.user_password, {'attr': {'id': 'passeword'}}) }}
                </div>

                <div class="col-12 inputInsc">
                    <label for="comfirmMdp">Comfirmer mot de passe : </label><input type="password" class="styl" id="comfirmMdp">
                </div>
            </div>
            {{ form_end(form) }}
            <button class="btn btn-primary" onclick="inscription()"> Rejoindre l'élite !</button>
        </div>
    </div>
</div>

<div id="alert" class="toast" role="alert" aria-live="assertive" aria-atomic="true" style="margin: 10px">
    <div class="toast-header">
        <strong class="mr-auto" style="font-size: 12px">Erreur !</strong>
        <button type="button" class="ml-2 mb-1 close" data-dismiss="toast" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>
    <div class="toast-body" style="font-size: 12px; background-color: dimgrey;">
        Les mots de passe ne sont pas identique !
    </div>
</div>
</body>

<script type="text/javascript" src="{{asset('js/util.min.js')}}"></script>
<script type="text/javascript" src="{{asset('js/jQuery.min.js')}}"></script>
<script type="text/javascript" src="{{asset('js/bootstrap.js')}}"></script>
<script type="text/javascript" src="{{asset('js/bootstrap.bundle.js')}}"></script>
<script type="text/javascript">

    $(document).ready(function () {
        $('.toast').toast({
            //delay: 2000
            autohide: false
        });
    });

    let inputs = $('input');
    /** Code By Webdevtrick ( https://webdevtrick.com )  **/
    inputs.on('focusin', function() {
        $(this).parent().find('label').addClass('active');
    });

    inputs.on('focusout', function() {
        if (!this.value) {
            $(this).parent().find('label').removeClass('active');
        }
    });

    /** Code By Webdevtrick ( https://webdevtrick.com )  **/
    inputs.on('focusin', function() {
        $(this).parent().find('label').addClass('active');
    });

    inputs.on('focusout', function() {
        if (!this.value) {
            $(this).parent().find('label').removeClass('active');
        }
    });

    function inscription() {
        let toast = document.getElementById("alert");
        toast.toast("hide");
        let form = document.getElementById("formInscription");
        let mdp = document.getElementById("passeword");
        let comfirm = document.getElementById("comfirmMdp");

        if(mdp === comfirm){
            //form.submit();
        }else{
            toast.toast("show");
        }
    }

</script>
</html>
